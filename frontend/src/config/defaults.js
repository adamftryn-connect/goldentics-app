/**
 * Fallback harga per gram (IDR) — selaras dengan entri terbaru di backend/data-gold.js.
 * Fallback jika API harga tidak tersedia (Prediksi & Kalkulator).
 */
export const FALLBACK_PRICE_PER_GRAM = 2_420_000;
